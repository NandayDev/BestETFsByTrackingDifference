# BestETFsByTrackingDifference

Simple client-side web app to extrapolate which ETFs are performing best, based on the tracking difference (difference between the index and the ETF performance based on NAV).
